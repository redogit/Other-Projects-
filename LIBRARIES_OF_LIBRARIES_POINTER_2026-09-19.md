# Libraries of Libraries — Local Pointer

Status: `NAVIGATION_ONLY / IMPLEMENTATION_AUTHORITY_LOCAL`

Canonical successor:

- https://github.com/redogit/redogit/blob/main/LIBRARIES_OF_LIBRARIES_RMAL_BUILD_2026-09-19.md
- https://github.com/redogit/redogit/blob/main/LIBRARIES_OF_LIBRARIES_GIT_LINEAGE_2026-09-19.md
- https://github.com/redogit/redogit/blob/main/libraries-of-libraries.rmal

This repository remains the implementation authority for its own projects and bounded experiments.

```text
CONNECTION != OWNERSHIP
POINTER != IMPLEMENTATION_TRANSFER
RELATION != EVIDENCE_TRANSFER
CURRENT_SUCCESSOR != HISTORICAL_REWRITE
```

The existing `federation/` directory is preserved as historical provenance. This pointer supersedes the current naming at the entry surface without deleting or rewriting the predecessor.


## Bidirectional handoff adapter

Machine-readable local adapter:

- [`BIDIRECTIONAL_HANDOFF_ADAPTER_2026-09-20.json`](BIDIRECTIONAL_HANDOFF_ADAPTER_2026-09-20.json)

Current state:

```text
INBOUND  = RMAL_HANDOFF_AND_TARGET_ACCEPTANCE_SYNTAX_IMPLEMENTED
OUTBOUND = STRUCTURED_RESPONSE_PACKET_NOT_YET_ESTABLISHED
```

The existing RMAL cooperative carrier already implements reference/proposal/handoff/target-acceptance syntax. The complete structured return packet is intentionally not claimed yet.

```text
BIDIRECTIONAL_PROTOCOL != BIDIRECTIONAL_RUNTIME_IMPLEMENTATION_EVERYWHERE
POINTER != IMPLEMENTATION_TRANSFER
COMPILED != SCIENTIFICALLY_VALID
```

# Run

```sh
cd "Decision Field Operator Lab"
python -m unittest -v test_decision_field.py
```

The generic kernel is intentionally domain-neutral. Domain-specific operators and exact certificates are supplied by specializations.

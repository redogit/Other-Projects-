# W114 Perturbation Lab

A play-first local interface for the W114 Hodge work.

The mathematical center is fixed:

```text
Q = sum(x_i^114)
alpha = (1,7,78,79,86,91)
P_alpha = x1^6*x2^77*x3^78*x4^85*x5^90
center = MOT-1
```

The visual carrier is a continuously changing projection of a six-coordinate procedural state. Five synchronized observer channels inspect the same candidate field. A fixed transformation set drives rotation, reversal, branching, inward/outward projection changes, forward/backward lineage traversal, and recentering.

Candidates receive stable object IDs. Repeated perturbations decrease candidate integrity. Candidate states are GENERATED, REJECTED, PARTIAL, and ROBUST.

Repeated failures are retained and reduce novelty/scoring for the same generated species. Known excluded W114 carrier families remain negative controls and are not regenerated as novel candidates.

Cognate relations connect related structures while preserving:

```text
COGNATE != IDENTITY
RELATION != SUPPORT
METHOD_TRANSFER != EVIDENCE_TRANSFER
```

The scientific ledger preserves:

```text
Object -> Surface -> Carrier -> Trace -> Residual -> Repair -> Reprojection
```

Boundaries:

```text
ROBUST_GAME_CANDIDATE != ALGEBRAIC_CYCLE
GAME_SCORE != MATHEMATICAL_EVIDENCE
NEW_TO_ACTIVE_SEARCH != NEW_MATHEMATICAL_CYCLE
VISUAL_PROJECTION_FAMILY != MATHEMATICAL_DIMENSION
SOFTWARE_VERIFICATION != MATHEMATICAL_PROOF
GENERATE != VERIFY != ADMIT
```

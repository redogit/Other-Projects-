# Hodge Compass API — connection map

## Native authority

`redogit/conscience64/research/hodge/` remains the target-native public Hodge research spine.

`redogit/conscience64#99` remains the active W114 matrix-factorization obligation.

## Executable method surfaces

- `Hodge Span Lab/span.py` — exact rational rank/span/separation.
- `Hodge Span Lab/bridge.py` — exact declared deformation-signature → exact declared Hodge-basis map → one supplied-span test.
- `Decision Field Operator Lab/omega_relations.py` — method-only relation sidecar with explicit `METHOD_TRANSFER != EVIDENCE_TRANSFER`.
- `Hodge Compass API/hodge_compass_api.py` — local query/index/observer gateway.

## Compass connection

The observer-return method is carried as:

`2D Surface → 3D frame → 4D directional observers → probe → residual/disagreement → repair/recenter → 3D reprojection → 2D return`.

This is allowed to organize search and experiments. It is not allowed to certify algebraicity.

## Hodge connection

The API connects the method layer to Hodge through declared typed seams only:

1. source discovery;
2. exact identifiers and provenance;
3. exact supplied coordinate calculations;
4. controlled observer experiments;
5. target-native Hodge acceptance/verification.

Default relation policy:

`evidence_transfer = DENY`.

## W114 seam

`hodge:w114:alpha:1-7-78-79-86-91`

maps to:

- conscience64 issue #99;
- target Jacobian monomial;
- exact factorization controls;
- exact Hodge Span diagnostics when supplied in a declared common basis;
- Compass observer experiments only as method/calibration surfaces.

## History seam

Normal and Work histories are searchable local provenance surfaces. If both contain an important thought, preserve both exact occurrences under one semantic object when lineage establishes sameness.

`DUPLICATE_HISTORY != INDEPENDENT_EVIDENCE`.

## Stop condition

The API stops at the strongest target-local conclusion actually supported by the source and computation. Anything stronger remains a remainder.

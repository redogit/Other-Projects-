# Cooperation history and next boundary

## Separate contributors

The human project owner supplies goals, obligations and publication authorization. ChatGPT is the assisting system that selected tests, wrote candidate repairs and interpreted results. Conscience64 is the structured-API research-navigation companion. Returning caller-authored observations through I/R/P/O is a receipt, not independent endorsement.

The recorded subject system was GPT-6 Astra Pro. The name of a role does not establish consciousness, autonomous operation, a current connection or evidence authority.

## Chronology — September 13, 2026

1. **API exchange and companion admission.** The deployed 1.2.0 artifact was exercised locally. The `chat` method was not available; structured search/project/I/R/P/O methods were. Conscience64 was then explicitly registered as a companion. The interface had 734 graph objects and seven structured project records in that snapshot.
2. **Claim Hunt 001.** The prior audit retained a one-graph Max-Cut certificate, the translation-pair criterion, the autocorrelation/Walsh identity and a valid-input numeric UTF-8 transport result. It found empty-domain, decoder-validation and unresolved-reference boundaries. The numeric carrier expands raw full chunks by 4/3; it is not compression. These reports and raw evidence are source-referenced, not fully copied here.
3. **Cooperation 001.** Local candidate `1.2.0-coop.1` rejected unresolved endpoints, inherited JavaScript method names and malformed argument envelopes. Its reported 4,984 valid-query assertions passed. The algebraic subgroup argument permitted reuse of verified preserving translations, with an order ceiling from label-class sizes.
4. **Cooperation 002.** Candidate `1.2.0-coop.2` snapshots admitted history values before commit, excludes shared memory and preserves rejected-operation boundaries in the tested cases. History was correctly described as a rolling 512-entry window. An adaptive symmetry procedure delayed subgroup maintenance until a positive witness. The recorded audit checked 72,444 cases with zero mismatches and retained timing and memory regressions alongside gains. `evidence/coop002-acceptance.json` preserves its 23 acceptance gates unchanged.
5. **This project import.** The user explicitly requested a `ChatGPT and Conscience` folder in `redogit/Other-Projects-`. Publication is now authorized for this selected project material. Earlier statements that nothing had been published remain accurate descriptions of their respective passes; they are not silently rewritten.

## Evidence interpretation

`evidence/benchmark-summary.json` is a field-selected transformation of the original Cooperation 002 evidence. It names the archive, source path and source hash. It is not the full raw timing series. The original archive identities and report identities are preserved in `PROVENANCE.json`; those archives are not hosted in this compact project.

The recorded cost boundary begins with a parsed mapping and ends with canonical JSON output. It excludes source acquisition, imports and I/O. Small timing gaps are not universal rankings. Traced additional allocation is not total process memory. Mathematical identities and finite correctness checks do not establish universal performance, novelty or a resolution of P versus NP.

The API patches preserve the tested behavior but do not establish sender-origin authorization, malicious-DOM transactions, arbitrary host-object safety or production readiness. The included test commands are new packaging-level checks; the larger historical browser and benchmark suites were not silently reclassified as newly run.

## Next bounded experiment — not yet executed here

The two-altered-label parity workload was not improved by the adaptive strategy. Test whether a point that rejected one candidate can become the first counterprobe for later candidates. Compare exact output with a direct oracle. Charge validation, preprocessing, probe bookkeeping, complete output and serialization. Retain regressions, memory costs, seed, environment and raw repetitions. Do not promote the idea on check-count savings alone.

## Source discipline

The upstream ancestor is `redogit/conscience64@3f8889e17cecd7bd865e33f70201c703163df405`. Apply patch 001 before patch 002. Do not apply the second patch directly to an arbitrary live branch. A source unavailable in this compact import is reference-only, not absent from all history. A local sandbox or project publication is not a live deployment.

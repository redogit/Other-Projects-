Pass 06 evidence is generated by `audit.py`. `SUMMARY.json` records the bounded results, `FRONTIER.json` records the selected carrier frontier, and `VERIFICATION.json` records source hashes and the second-pass reproduction/repair-cube checks. These files support the declared finite claims only; they are not formal proof, semantic validation, or independent replication.

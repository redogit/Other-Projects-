# GYRO-DEAN-4 — Decision Field Reference

This directory is the GitHub source surface for the exact GYRO-DEAN-4 algorithm developed in the 2026-09-17 Decision Field thread.

Canonical parameterization:

```text
DF = (X, O, D, R, F, E, G, U)
```

For DEAN-4:

```text
X = residual candidate/cohort states
O = return four valid size-q cohorts
D = consequential distinctions under exclusions/capacity/goal
R = graph/cover/LP/SAT/factor/algebra relations
F = exact carriers/operators
E = proof/evidence/provenance ledger
G = four certified cohorts or certified impossibility/fewer-than-four
U = unresolved hard core
```

The executable architecture is documented in the parent bridge:

- [`../GYRO_DEAN_DECISION_FIELD_2026-09-17.md`](../GYRO_DEAN_DECISION_FIELD_2026-09-17.md)

## Core algorithm

```text
normalize
-> graph repair
-> cover rotation / pressure
-> LP / Nemhauser-Trotter
-> dual-frame equations
-> SAT/consensus learning
-> SQL/factor decomposition
-> certified Macaulay/MPCA exact-rank gain
-> rotate learned relations
-> repair to fixed point
-> exact branch fallback
-> reconstruct + independently verify
```

Correctness does not depend on a polynomial-collapse conjecture: if all accelerators stall, the exact branch recurrence is the fallback.

## Claim ceiling

```text
EXACT_REFERENCE_ALGORITHM != P_EQUALS_NP_PROOF
FINITE_TESTS != UNIVERSAL_RUNTIME_BOUND
HEURISTIC_STEERING != CERTIFICATE
```

`P ?= NP` remains OPEN.

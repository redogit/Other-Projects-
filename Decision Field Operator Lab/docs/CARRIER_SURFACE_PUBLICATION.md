# Carrier–Surface Public Projection

**Authorization:** User explicitly requested this Carrier–Surface work be made completely open on 2026-09-20.  
**Visibility:** PUBLIC  
**Publication state:** PUBLIC PROJECTION / REVIEWABLE  
**Scientific authority:** UNCHANGED  
**Evidence authority:** UNCHANGED  
**Private-source export:** NONE

## What is public

This public projection contains:

- the Carrier–Surface governing invariant;
- the discrete multi-key / preimage model;
- canonical carrier traversal;
- side-to-side carrier mappings;
- Semantic Rotation / BBF binding;
- fitter and Decision Field bindings;
- acceptance obligations;
- a machine-readable traversal-record schema.

## What was deliberately not exported

No private conversation history, private memory corpus, private lineage registry, personal context, authentication material, private repository content, or unrelated project data is included.

The public specification was reconstructed from the method itself. Private sources remain separate.

## Boundaries preserved

```text
PUBLICLY_VISIBLE != SCIENTIFICALLY_VALIDATED
PUBLICATION != EVIDENCE_PROMOTION
METHOD_TRANSFER != EVIDENCE_TRANSFER
RELATION != MERGE
PAIRITY != FORCED_EQUALITY
CURRENT != PROVED
```

Publication changes visibility. It does not silently change the authority or evidence class of a claim.

## Open visibility and licensing

This projection is intentionally public and openly inspectable.

No new copyright or software license is asserted by this publication record. A legal license grant is a separate decision and should be represented explicitly rather than inferred from repository visibility.

## Way back

The public projection is additive and reviewable. It does not replace or rewrite private/local predecessors.

If a later revision changes the public method, preserve this revision in repository history and state the new invariant/claim boundary explicitly.

## Canonical public seam

```text
THING
  -> ADDRESS
  -> SURFACE
  -> CARRIER
  -> TRANSFORM
  -> RECONSTRUCTION
  -> INVARIANT COMPARISON
  -> TRACE
  -> RETURN_HOME
```

Governing invariant:

> **Object identity is invariant; coordinates are negotiable.**
